class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        j=list(tuple(J))
        count=0
        for i in S:
            if(i in j):
                count=count+1
        return count
